[package]
name = "vrac"
version = "0.0.0"
description = "Native Tauri shell for Vrac"
authors = ["SilloVV"]
license = "Apache-2.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
